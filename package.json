{
  "name": "pr-sentinel",
  "version": "0.1.0",
  "description": "A lightweight PR risk scanner for code review automation.",
  "type": "module",
  "bin": {
    "pr-sentinel": "./bin/pr-sentinel.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check bin/pr-sentinel.js && node --check src/diff-parser.js && node --check src/formatters.js && node --check src/github.js && node --check src/rules.js && node --check src/scanner.js && node --check src/severity.js && node --check test/diff-parser.test.js && node --check test/scanner.test.js && node --check test/severity.test.js",
    "scan:sample": "node bin/pr-sentinel.js --diff test/fixtures/sample.diff --format markdown --fail-on none"
  },
  "keywords": [
    "github-action",
    "pull-request",
    "code-review",
    "risk-scanner",
    "ci"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "files": [
    "bin",
    "src",
    "action.yml",
    "README.md",
    "LICENSE"
  ]
}
